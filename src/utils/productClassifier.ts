import type {InsuranceCategory,SourceKind} from '../types/product';
export function classifyProduct(productName:string, declared?:string):InsuranceCategory{const t=`${declared||''} ${productName}`; if(/변액.*종신|변액종신/.test(t))return '변액종신보험'; if(/변액.*정기|변액정기/.test(t))return '변액정기보험'; if(/변액.*연금|변액연금/.test(t))return '변액연금보험'; if(/변액|유니버셜/.test(t)&&/저축|적립|키즈드림/.test(t))return '변액저축보험'; if(/CI|GI|중대한|선지급|라이프케어|진단보험금|미리받는|하나로H종신/.test(t))return 'CI/GI보험'; if(/치매|간병|장기요양|LTC|간병인지원|요양병원/.test(t))return '간병/치매보험'; if(/암|항암|암진단|암수술|암주요치료|유사암|고액암/.test(t))return '암보험'; if(/건강|질병|수술|입원|뇌혈관|허혈성|심근경색|뇌출혈|간편건강|뇌심/.test(t))return '질병/건강보험'; if(/정기|경영인정기|e정기|정기사망/.test(t))return '정기보험'; if(/종신|상속|사망보험금|H종신|제로백/.test(t))return '종신보험'; if(/상해|재해|골절|교통재해|레저|부상|장해/.test(t))return '상해보험'; if(/어린이|자녀|아이|태아|키즈/.test(t))return '어린이보험'; if(/치아|치과|임플란트|보철|크라운/.test(t))return '치아보험'; if(/연금저축|e연금저축|하이드림연금/.test(t))return '연금저축보험'; if(/연금/.test(t))return '연금보험'; if(/저축|재테크|목돈마련|디딤돌|DREAM/.test(t))return '저축보험'; return (declared as InsuranceCategory)||'기타보장성보험'}
export function sourceKindFor(category:InsuranceCategory):SourceKind{if(category==='변액종신보험'||category==='변액정기보험')return '변액보장성보험'; if(category==='변액저축보험'||category==='변액연금보험')return '변액저축성보험'; if(category==='연금저축보험')return '연금저축보험'; if(category==='저축보험'||category==='연금보험')return '저축보험'; return '보장성보험'}
export function tagsFor(company:string,category:InsuranceCategory,name:string,benefits:string[]){const t=new Set<string>([company,category.replace('보험','')]); const raw=`${name} ${benefits.join(' ')}`; if(/사망/.test(raw))t.add('사망보장'); if(/납입면제/.test(raw))t.add('납입면제'); if(/암/.test(raw))t.add('암'); if(/AI/.test(raw))t.add('AI'); if(/암주요치료/.test(raw))t.add('암주요치료'); if(/간병|요양/.test(raw)){t.add('간병');t.add('요양')} if(/치매/.test(raw))t.add('치매'); if(/정기/.test(raw))t.add('정기'); if(/e|인터넷|온라인/.test(raw))t.add('온라인'); if(/변액/.test(raw))t.add('변액'); if(/유니버셜/.test(raw))t.add('유니버셜'); if(/저축|적립/.test(raw))t.add('저축'); if(/연금/.test(raw))t.add('연금'); return [...t]}
