import type {Customer} from '../types/customer';import type {Product} from '../types/product';
export function generateScript(c:Customer,p:Product,purpose:string,tone:string){const soft=tone==='친근한'||tone==='부드러운';return {start:`${c.name} 고객님, 오늘은 현재 상황에 맞춰 꼭 필요한 보장부터 차분히 점검해보겠습니다.`,need:`${c.family} 상황과 ${c.recentEvents.join(', ')} 이슈를 보면 ${c.interests.join(', ')} 중심의 우선순위 설정이 필요합니다.`,compare:`단순히 보험료만 보기보다 ${p.category}의 보장 범위, 납입면제, 상담 활용도를 같이 비교하는 것이 좋습니다.`,hanwha:p.insurer==='한화생명'?`${p.name}은 고객님의 니즈를 가족보장·건강리스크·향후 활용 관점으로 연결해 설명하기 좋습니다.`:`타사 상품과 함께 비교하되, 한화생명 전략 상품도 같은 기준으로 검토해 보겠습니다.`,objection:`보험료 부담이 우려되시면 기존 보험의 중복을 줄이고 부족한 보장만 보완하는 ${purpose} 방식으로 접근하겠습니다.`,close:soft?'부담 없이 비교표를 먼저 보시고 다음 상담에서 세부 설계를 조정해보시죠.':'오늘 비교 결과를 기준으로 제안서 요약본을 만들고 다음 액션을 확정하겠습니다.'};}
